apiVersion: v1
kind: Service
metadata:
  name: dynatrace-php-demo
  namespace: mynamespace
spec:
  selector:
    app: dynatrace-php-demo
  ports:
    - name: http
      port: 80
      targetPort: 80
  type: ClusterIP
